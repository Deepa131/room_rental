import 'package:flutter/material.dart';

class OwnerRequestScreen extends StatelessWidget {
  const OwnerRequestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Owner Requests"));
  }
}
